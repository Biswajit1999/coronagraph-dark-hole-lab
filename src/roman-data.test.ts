import{describe,expect,it}from"vitest";import data from"../public/data/roman-hlc-os11.json";describe("real Roman OS11 derivative",()=>{it("binds the derivative to archive and FITS hashes",()=>{expect(data.source.expectedBytes).toBe(448088109);expect(data.source.sha256).toMatch(/^[a-f0-9]{64}$/);expect(data.source.memberSha256).toMatch(/^[a-f0-9]{64}$/);expect(data.source.member).toContain("sky_transmission")});it("retains physical map limits",()=>{expect(data.fits.shape).toEqual([275,275]);expect(data.fits.minimum).toBeGreaterThanOrEqual(0);expect(data.fits.maximum).toBeLessThanOrEqual(1.01);expect(data.grid).toHaveLength(128)})});
