import{useEffect,useState}from"react";import"./styles.css";
import"./overrides.css";
type Data={source:{url:string;expectedBytes:number;sha256:string;member:string;memberSha256:string;archivePage:string;credit:string};fits:{shape:number[];header:Record<string,string>;minimum:number;maximum:number;median:number};preview:string;grid:number[][];warning:string};
export default function App(){const[data,setData]=useState<Data|null>(null);const[error,setError]=useState("");const[pos,setPos]=useState({x:68,y:52});useEffect(()=>{fetch(`${import.meta.env.BASE_URL}data/roman-hlc-os11.json`).then(r=>{if(!r.ok)throw Error(`${r.status}`);return r.json()}).then(setData).catch(e=>setError(String(e)))},[]);if(error)return <main className="state">OS11 receipt error: {error}</main>;if(!data)return <main className="state">Opening the Roman CGI field map…</main>;const row=Math.min(data.grid.length-1,Math.floor(pos.y/100*data.grid.length));const col=Math.min(data.grid[0].length-1,Math.floor(pos.x/100*data.grid[0].length));const transmission=data.grid[row][col];return <><header><a href="#top">ROMAN CGI / HLC / OS11 V3</a><nav><a href="#field">field</a><a href="#meaning">meaning</a><a href="#receipt">receipt</a></nav></header><main id="top"><section className="hero"><p>OFFICIAL SIMULATED FLIGHT-DESIGN PRODUCT · CORRECTED 2024 GEOMETRY</p><h1>A dark hole is<br/><i>not equally transparent.</i></h1><aside>Contrast tells you how much starlight remains. Throughput tells you whether an off-axis planet survives the coronagraph. A credible sensitivity map needs both.</aside></section><section className="field" id="field"><div className="map-wrap"><button className="map" aria-label="Select a position on the Roman HLC sky-transmission map" onClick={e=>{const r=e.currentTarget.getBoundingClientRect();setPos({x:(e.clientX-r.left)/r.width*100,y:(e.clientY-r.top)/r.height*100})}}><img src={`${import.meta.env.BASE_URL}${data.preview}`} alt="Roman HLC OS11 off-axis sky transmission map"/><span className="probe" style={{left:`${pos.x}%`,top:`${pos.y}%`}}/></button><div className="map-axis"><span>− field angle</span><span>+ field angle</span></div></div><div className="readout"><span>PROBE / APPROXIMATE DERIVED GRID</span><strong>{(transmission*100).toFixed(1)}<small>%</small></strong><dl><div><dt>x / preview</dt><dd>{pos.x.toFixed(1)}%</dd></div><div><dt>y / preview</dt><dd>{pos.y.toFixed(1)}%</dd></div><div><dt>FITS source</dt><dd>{data.fits.shape.join(" × ")}</dd></div><div><dt>map maximum</dt><dd>{(data.fits.maximum*100).toFixed(1)}%</dd></div></dl><p>Click anywhere in the map. The readout samples a compact 128² derivative; use the original FITS product for quantitative work.</p></div></section><section className="meaning" id="meaning"><div><span>THE RESEARCH GAP</span><h2>Control-region geometry and science throughput belong in the same audit.</h2></div><div className="statements"><article><b>01</b><h3>Not a contrast map</h3><p>Each pixel gives the relative peak of an off-axis PSF centered there, normalized by the field maximum.</p></article><article><b>02</b><h3>Not on-sky data</h3><p>OS11 is an instrument-team simulation including a declared observing scenario, not a Roman observation.</p></article><article><b>03</b><h3>Not a detection limit</h3><p>Planet sensitivity also needs residual speckles, post-processing, detector noise, spectrum, roll strategy, and completeness.</p></article></div></section><section className="receipt" id="receipt"><div><span>448 MB → ONE TRACEABLE MAP</span><h2>The archive stays attached.</h2></div><ol><li><b>01</b><div><strong>Official OS11 v3 archive</strong><a href={data.source.url}>{data.source.expectedBytes.toLocaleString()} bytes ↗</a></div></li><li><b>02</b><div><strong>Archive SHA-256</strong><code>{data.source.sha256}</code></div></li><li><b>03</b><div><strong>Exact ZIP member</strong><code>{data.source.member}</code></div></li><li><b>04</b><div><strong>FITS member SHA-256</strong><code>{data.source.memberSha256}</code></div></li></ol></section><aside className="warning"><b>VALIDITY BOUNDARY</b><p>{data.warning}</p></aside></main><footer><span>{data.source.credit}</span><a href={data.source.archivePage}>ROMAN PUBLIC SIMULATIONS ↗</a></footer></>}
